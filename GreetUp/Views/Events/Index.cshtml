@model IEnumerable<greet_up.Models.Events>

@{
    ViewBag.Title = "Events";
}

<h2>Events</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>

<div class="row">

    @foreach (var item in Model)
    {
        <div class="col-sm-6 col-md-4">
            <div class="thumbnail">
                <div class="caption">

                    <h3> @Html.DisplayFor(modelItem => item.EventName)</h3>
                    @*@Html.DisplayFor(modelItem => item.EventDate)
                    @Html.DisplayFor(modelItem => item.EventLocation)*@
                    <p>@Html.DisplayFor(modelItem => item.EventDescription)</p>
@*                    @Html.DisplayFor(modelItem => item.EventAttendee)*@


 @*                   @Html.ActionLink("Edit", "Edit", new { id = item.EventID }) |*@
                    <p>@Html.ActionLink("Details", "Details", new { id = item.EventID }, new { @class = "btn btn-primary" })</p>
@*                    @Html.ActionLink("Details", "Details", new { id = item.EventID }) |*@
  @*                 @Html.ActionLink("Delete", "Delete", new { id = item.EventID })*@
                </div>
            </div>
        </div>
    }
</div>



                     @*<p>@Html.ActionLink("Details", "Details", new { id = item.ID }, new { @class = "btn btn-primary" })</p>*@
                

